/**
 *      Operadores Logicos
 */

//And, Or, y Not - Y, O, Negacion(NO)

// && : Operador Y
// || : Operador 0
// !  : Operador De Negacion

const a = 10 ;
const b = 20;
const c = "10";

(a == b && a === c) // false
(a != b || a == c) //true
!(a === c) //falso o verdadero